# src/utils — 通用工具

跨模块共享的基础设施。**判断标准**：某段逻辑被 ≥2 个模块需要，才移入此处。

## 规划模块

- `io.py` — 图像读写统一封装（尺寸/格式断言、EXIF 方向处理——手机拍摄的 4K 图可能有 EXIF 旋转陷阱）
- `logging.py` — 统一日志格式（run_id 贯穿）
- `seed.py` — 全局随机种子管理
- `image_ops.py` — 通用的读图/缩放断言/尺寸工具函数

## 约定

- 本目录不依赖项目其他模块（保持最底层）
- 纯函数优先，方便 `tests/` 覆盖
