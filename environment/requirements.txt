# 本项目自身依赖（远程 5090，Python 3.10；torch 由 setup_5090.sh 单独装 cu128 版）
# 注意：DiffBIR/ResShift 的依赖在 setup_5090.sh 中从其仓库 requirements 安装，不在此处。

numpy
pillow>=10
pyyaml
tqdm
pytest
huggingface_hub
pyiqa          # LPIPS/DISTS/MUSIQ/CLIPIQA/NIQE/MANIQA（会带 timm 等依赖）
torchsde       # DiffBIR v2 的 k-diffusion 采样器依赖（勿被 torch 前缀误伤）
setuptools<81  # clipiqa -> OpenAI clip 包依赖 pkg_resources（新版已移除，排障 #17）
